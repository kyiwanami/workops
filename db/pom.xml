<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>com.example.workops</groupId>
	<artifactId>workops-db</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<packaging>pom</packaging>
	<name>workops-db</name>

	<properties>
		<flyway.version>12.9.0</flyway.version>
		<mysql.connector.version>9.7.0</mysql.connector.version>
		<workops.flyway.location.migration>filesystem:migration</workops.flyway.location.migration>
		<workops.flyway.location.seed.common>filesystem:seed/common</workops.flyway.location.seed.common>
	</properties>

	<build>
		<plugins>
			<plugin>
				<groupId>org.flywaydb</groupId>
				<artifactId>flyway-maven-plugin</artifactId>
				<version>${flyway.version}</version>
				<!-- This project only runs Flyway against the SQL directories under db/. -->
				<configuration>
					<url>${env.WORKOPS_DB_URL}</url>
					<user>${env.WORKOPS_DB_USERNAME}</user>
					<password>${env.WORKOPS_DB_PASSWORD}</password>
					<locations>
						<location>${workops.flyway.location.migration}</location>
						<location>${workops.flyway.location.seed.common}</location>
						<location>${workops.flyway.location.seed.profile}</location>
					</locations>
				</configuration>
				<dependencies>
					<dependency>
						<groupId>org.flywaydb</groupId>
						<artifactId>flyway-mysql</artifactId>
						<version>${flyway.version}</version>
					</dependency>
					<dependency>
						<groupId>com.mysql</groupId>
						<artifactId>mysql-connector-j</artifactId>
						<version>${mysql.connector.version}</version>
					</dependency>
				</dependencies>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>local</id>
			<properties>
				<workops.flyway.location.seed.profile>filesystem:seed/local</workops.flyway.location.seed.profile>
			</properties>
		</profile>
		<profile>
			<id>dev</id>
			<properties>
				<workops.flyway.location.seed.profile>filesystem:seed/dev</workops.flyway.location.seed.profile>
			</properties>
		</profile>
	</profiles>
</project>
